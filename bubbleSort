List=[5,8,1,3,2]
swap=True
while swap:
    swap=False
    for i in range(len(List)-1):
        if List[i] > List[i + 1]:
            List[i],List[i+1]=List[i+1],List[i]
            swap=True
            print(List)
